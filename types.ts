export interface IExpenseType {
    id: string;
    name: string;
    amount: string;
    percentage: string;
  }

  export interface IncomeType {
    id: string;
    name: string;
    amount: string;
  }

  export interface ISpendingType {
    id: string;
    name: string;
    amount: string;
    date: string;
  }